
/* eslint @typescript-eslint/no-unused-vars: off */
/* eslint @typescript-eslint/no-explicit-any: off */

export function validation(account: string) {
  return true;
  // var checkSumAddresses: Array<string> = []
  // WHITELIST_IP_ADDRESSES.map((ad) => {
  //   checkSumAddresses.push(getAddress(ad).toLowerCase())
  // })
  // console.log("validation ", account, process.env.REACT_APP_DEV_ENV, checkSumAddresses)
  // if (process.env.REACT_APP_DEV_ENV === 'production' || process.env.REACT_APP_DEV_ENV === 'test') return true;
  // if (checkSumAddresses.includes(account)) {
  //   console.log("validation ", "includes")
  //   return true;
  // } else {
  //   console.log("validation ", "not includes")
  //   return false;
  // }
}